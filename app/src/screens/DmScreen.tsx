// TODO: schermata conversazione privata 1:1 (DM).
// Accessibile da Strato 2 in poi (vedi permessi_e_strati.md §1).
//
// Funzionalità previste:
// - Lista conversazioni DM attive (con ultimo messaggio e timestamp)
// - Apertura di una conversazione singola
// - Invio e ricezione messaggi in tempo reale (Supabase realtime)
// - Richiesta DM: la destinataria deve accettare prima di poter scrivere
// - Mostrare stato richiesta (in attesa / accettata / rifiutata)
// - Rispettare il filtro DM della destinataria (messaggio generico se bloccata)
// - Block: se una delle due blocca l'altra, la conversazione si interrompe

export function DmScreen() {
  return null
}
